<p style="color: green"><%= notice %></p>

<h1>Orders</h1>
<%= render partial: "orders/form", locals: { order: Order.new } %>

<div id="orders">
  <% @orders.each do |order| %>
    <%= render order %>
    <p>
      <%= link_to "Show this order", order %>
    </p>
  <% end %>
</div>

<%= link_to "New order", new_order_path %>
